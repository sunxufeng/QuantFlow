# 2026-08-14 QuantFlow 进展记录

## 本日完成
- **UI 调整：回测报告中心 + 工作流节点紧凑化**
  - 回测报告中心：标题 15px、表格 12px/表头 11px、指标值 17px、曲线 120px。
  - 工作流节点：`qf-node` 基准字号从 12px 降到 11px，节点最小宽度 150px，标题/分类/参数/输入框/输出统一缩小并收紧间距。
  - 全局一致性：监控页指标卡值 24px→18px、模板库标题 18px→15px，排版层级统一。
  - 模拟交易页（Trading.jsx）：区块标题 14px→13px、切换/买卖按钮 13px→12px、权益曲线高度 160→120px（页面标题 h2、指标卡值已随前面两轮自动生效）。

## 重要坑点补充
- **V1.8 已有完整「模拟交易」模块**，不要重复造轮子**：`frontend/src/Trading.jsx`（侧边栏 key='trade'「模拟交易」）+ `backend/app/api/trading.py` + `backend/app/trading/engine.py` + `backend/app/trading/store.py` 已实现市价/限价下单、持仓、委托、模拟推进、分析。本次一度误建 `backend/app/trading/paper.py`（回测重放）并覆盖 `trading/__init__.py`，已 `git checkout` 还原并删除冗余文件。
- **`backend/app/trading/__init__.py` 只能保留原始 docstring**，切勿写入 `from .paper import ...` 之类——否则会破坏 `api/trading.py` 对 `engine`/`store` 的导入（启动即 500）。
- 前端页面头部多用内联 `fontSize`，全局改 CSS 不会覆盖这些内联值，需逐页 Edit 内联字号。
  - 新增 `.qf-reports*`、`qf-table`、`qf-an-block`、`qf-an-title` 样式；报告页指标卡值从 24px 降到 17px，表格字体 12px、表头 11px、padding 收紧。
  - `BacktestReports.jsx` 标题改为 `qf-reports-title`（15px），tab 按钮改用 CSS 类，净值曲线高度从 160 降到 120，操作按钮改为 "查看/取消/对比" 紧凑文案。
  - 前端构建通过并已部署到 `116.62.188.165:8080`（CSS 文件 `index-l3pSj8uG.css`）。
- **V2.4 部署上线 (v2.10.0)**：行情看板与自选股监控（watchlist/quotes 路由、MarketBoard.jsx、模拟实时跳动）。生产已验证。
- **V2.5 因子库扩充与因子评分 (v2.11.0)**：
  - 新增 `backend/app/factors/registry.py`（7 个因子：momentum/volatility/rsi/mean_reversion/volume_trend/drawdown/sharpe）+ `scoring.py`（横截面 rank/zscore 标准化 + 方向 + 权重 → 综合分排名）。
  - 新路由 `GET/POST /api/factors/scoring/*`（**注意**：不得覆盖 V1.1 已有的 `/api/factors/library` 与 `/api/factors/analyze`，二者在 `backend/app/api/factors.py`）。
  - 前端 `Factors.jsx` + 侧边栏"因子评分"入口；SDK `factor_scoring_catalog`/`score_factors`（0.4.0）。
  - 测试 407 passed（含 5 个新增）。生产验证：4 标的 × 7 因子全部非 None，排名有效。

## 关键坑点（重要，避免再踩）
1. **误覆盖风险**：`backend/app/api/factors.py` 与 `backend/app/factors/__init__.py` 是 V1.1 因子库/分析模块（analyzer/library/stats/transform）。新增因子能力务必用独立路由 `factor_scoring.py`，不要重写这些文件。`git checkout HEAD --` 可快速回滚误覆盖。
2. **合成行情只有 20 个交易日**（2024-01-02 ~ 2024-01-29；FUTURE 到 02-01）。因子默认窗口必须 ≤10 才能算出值（20 日窗口会因 `len <= window` 返回 None）。`DEFAULT_START=2024-01-01`、`DEFAULT_END=2024-02-01`。
3. **Git 协议被代理拦截**（git push → 502 CONNECT）。改用 `push_api.py` 走 GitHub Git Database REST API（blobs→tree(base_tree)→commit→PATCH ref）。记得先把 MESSAGE 改成本次版本，并把变更文件作为命令行参数传入。
4. **本地 SQLite 无行情数据**：`market_service.bars` 在本地仓库为空时回退到 tushare 报错；测试用 conftest 预置的测试库，生产用已同步进 SQLite 的 fixture。本地手测因子需走服务端 curl 验证。
5. **watchlist DELETE 是路径参数** `/api/market/watchlist/{symbol}`（非 query）；optimizer 请求用顶层 `symbols/start/end`，`fixed_params/grid` 分开。

## 当前阻塞（均待用户凭证，已多次告知）
- **V2.6 tushare 真实行情源**（看板任务 rZNEvL）：待用户提供 `QF_TUSHARE_TOKEN`。
- **真实券商 SDK 实盘下单**（rJuJtg）：待用户提供券商凭证（universal/easytrade/xuntou + api_key/secret/base_url/account_id）。用户此前明确表示暂时无法提供。
- 上述两项在凭证到位前无法推进；V2.x 其它任务已全部完成。

## 环境速查
- 仓库 `/Users/sunxufeng/.../quantflow`；远程 `github.com/sunxufeng/QuantFlow`。
- 生产 `116.62.188.165:8080`，部署 `bash deploy/deploy-remote.sh`（tar-pipe SSH app/+前端 dist，重启 quantflow.service，health 探测）。
- 飞书通知：`lark-cli im messages-send --chat-id oc_ea45f82679bd3c90715d83da8a46f247 --as bot --markdown "$MSG"`。
- 看板：`mcp__wb-issues__todo_show/transition`，父任务"V2.x 后续迭代" = rvai4j。
- 生产鉴权：先 `/api/auth/login` 拿 token（测试用户 smoke_qa/secret123 可注册）。

## 本日追加（V2.7 预警自动巡检调度，离线可做）
- 新增 `backend/app/alerts/scheduler.py`：独立 `BackgroundScheduler`，按 `QF_ALERT_EVAL_INTERVAL_MINUTES`（默认 2 分钟，min 1）周期调用 `alert_service.evaluate_all()`；`QF_DISABLE_ALERT_SCHEDULER=1` 可关；暴露 `start()/shutdown()/status()`。`_run()` 懒加载 `alert_service` 且吞异常。
- `backend/app/main.py` startup/shutdown 挂载 alerts scheduler（包 try/except 容错）。
- `backend/app/api/alerts.py` 新增 `GET /api/alerts/scheduler`（status：running/interval_minutes/disabled）；`backend/tests/test_alerts.py` 加 `test_alert_scheduler_status`。
- 前端：`alertSchedulerStatus()`（api.js）、`Alerts.jsx` 拉取并在标题旁显示「自动巡检·N分钟」徽标（`.qf-badge` 样式新增）；SDK `get_alert_scheduler()`。
- `APP_VERSION` → 2.12.0。
- 用途：让 V2.3 预警引擎在生产中**自动化运行**（此前仅手动 `/api/alerts/evaluate`），命中即走通知渠道推送。纯离线。
- 状态：**代码已完成，待部署**。注意：本时段 Bash 工具不可用（git/npm/pytest/deploy/lark-cli 均无法执行），未能构建/推送/部署/发飞书。Bash 恢复后立即 commit→push（push_api.py）→build→deploy→冒烟→发飞书→看板标记完成。

## 本时段环境异常（重要）
- **Bash 工具在本轮不可用**：所有 `command` 调用均返回 `Command rejected: Parameter "command" expected string, but received undefined`。因此无法执行 git/npm/pytest/deploy/curl/lark-cli。文件编辑类操作（Read/Write/Edit/Grep/Glob）与 MCP 看板工具正常。待 Bash 恢复后需补：构建前端、跑后端测试、REST API 推送、deploy、生产冒烟、飞书通知。

