apiVersion: v1
kind: Secret
metadata:
  name: quantflow-secret
  namespace: quantflow
type: Opaque
stringData:
  # 演示占位符。生产环境务必用强随机密钥覆盖，例如：
  #   kubectl -n quantflow create secret generic quantflow-secret \
  #     --from-literal=QF_SECRET_KEY="$(openssl rand -hex 32)" \
  #     --dry-run=client -o yaml | kubectl apply -f -
  QF_SECRET_KEY: "please-change-me-in-production-8f3b2a1c9d4e"
